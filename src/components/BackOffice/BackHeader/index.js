import React from 'react';
import PropTypes from 'prop-types';

import './styles.scss';

const BackHeader = () => (
  <div className="backHeader">BackHeader</div>
);

BackHeader.propTypes = {};

export default BackHeader;
